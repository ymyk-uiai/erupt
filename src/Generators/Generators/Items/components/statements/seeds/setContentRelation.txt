<!erupt
instance = self.files.model.instance
instances = self.files.model.instances
authInstance = auth.files.model.instance
authInstances = auth.files.model.instances
!>

<!erupt
    print("foreach(${} as ${}) {", instances, instance)
    print("${} = ${}->random();", authInstance, authInstances)
    print("${}->{}()->save(${})", authInstance, instances, instance)
!>

<data>
{
    "name": "setContentRelation"
}
</data>