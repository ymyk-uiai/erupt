<!erupt
    foreach self.relationships.seeds as seed join "\n" into result {
 
        instances = seed.files.model.instances
        class_name = seed.files.model.class_name
 
        print("${} = {}::all();", instances, class_name)
    }
!>

<data>
{
    "name": "getAllRelatedInstances"
}
</data>