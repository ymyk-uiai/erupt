<!erupt
    authClass = auth.files.model.class_name
    authInstance = auth.files.model.instance
    modelClass = self.files.model.class_name
    modelInstance = self.files.model.instance
!>

<!erupt
    print("public function forceDelete({} ${}, {} ${})", authClass, authInstance, modelClass, modelInstance)
!>
{
    <!erupt
        print("return ${}->id === ${}->user_id;", authInstance, modelInstance)
    !>
}

<data>
{
    "name": "forceDelete",
    "type": "php_class",
    "components": [],
    "use": [
        "<!erupt auth.files.model.fullClassName !>",
        "<!erupt self.files.model.fullClassName !>"
    ]
}
</data>