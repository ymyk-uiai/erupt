public function rules()
{
    <!erupt
        foreach self.props.fillable as fillable join "," into result {
            foreach fillable.validationRules as rule join "|" into res {
                print("{}", rule,name)
            }
            print("'{}' => '{}'", fillable.name, res)
        }
        print("return [{}];", result)
    !>
}

<data>
{
    "type": "php/request"
}
</data>