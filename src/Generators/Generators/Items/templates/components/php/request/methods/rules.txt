public function rules()
{
    <!erupt
        foreach self.props.ordinary.resource as resource join "," into result {
            foreach resource.validationRules as rule join "|" into res {
                print("{}", rule,name)
            }
            print("'{}' => '{}'", resource.name, res)
        }
        print("return [{}];", result)
    !>
}

<data>
{
    "type": "php/request"
}
</data>