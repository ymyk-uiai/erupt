<!erupt
    foreach self.attributes.fillable as input join ", " into result {
        preprint("protected $fillable = [")
        print("'{}'", input.name)
        postprint("];")
    }

    print("{}", result)
!>

<!erupt
    foreach self.props.ordinary.resource as resource join ", " into result {
        print("'{}'", resource.name)
    }
    print("protected $fillable = [{}];", result)
!>

<data>
{
    "type": "php/model",
    "components": [],
    "use": []
}
</data>