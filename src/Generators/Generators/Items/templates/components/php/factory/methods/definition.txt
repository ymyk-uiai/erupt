public function definition()
{
    <!erupt
        foreach self.props.needFactoryValue as needFactoryValue join "," into result {
            print("'{}' => {}", needFactoryValue.name, needFactoryValue.factory)
        }
        print("return [{}];", result)
    !>
}

<data>
{
    "type": "factory/definition",
    "components": [],
    "use": []
}
</data>