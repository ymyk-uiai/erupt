public function toArray($request)
{
    <!erupt
        foreach self.attributes.fillable as fillable join "," {
            preprint("return [")
            print("'{}' => $this->{}", fillable.name, fillable.name)
            postprint("];")
        }
    !>
}